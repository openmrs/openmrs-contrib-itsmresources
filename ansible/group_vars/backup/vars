---
docker_args: "--data-root=/data/docker"

backup_tag: 'bootstrapped'
backup_user: 'backup-s3'
users:
 "{{backup_user}}":
   comment: Backup User
aws_output_format: 'json'
aws_region: 'us-west-2'
aws_cli_user: '{{backup_user}}'


datadog_checks:
  backups_pending_check:
    init_config:
    instances:
        - foo: bar
