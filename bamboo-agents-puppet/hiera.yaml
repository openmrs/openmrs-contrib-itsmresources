---
version: 5
defaults:
  datadir: /etc/puppet/hieradata
  data_hash: yaml_data
hierarchy:
  - name: "Per environment config"
    paths:
      - "environments/%{::environment}.yaml"
  - name: "Common config"
    paths:
      - "common.yaml"
